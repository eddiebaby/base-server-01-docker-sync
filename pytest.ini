[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Configure test discovery paths
testpaths = tests

# Enable verbose output
addopts = -v

# Prevent pytest from recursively searching for tests in parent directories
norecursedirs = .* build dist CVS _darcs {arch} *.egg venv env